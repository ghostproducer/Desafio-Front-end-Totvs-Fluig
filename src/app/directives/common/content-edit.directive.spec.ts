import { ContentEditDirective } from './content-edit.directive';

describe('ContentEditDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new ContentEditDirective();
    expect(directive).toBeTruthy();
  });
});
